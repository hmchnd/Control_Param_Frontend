sap.ui.define([
    "sap/ui/test/opaQunit"
], function (opaTest) {
    "use strict";

    var Journey = {
        run: function() {
            QUnit.module("First journey");

            opaTest("Start application", function (Given, When, Then) {
                Given.iStartMyApp();

                Then.onThee_procurement_control_paramTList.iSeeThisPage();

            });


            opaTest("Navigate to ObjectPage", function (Given, When, Then) {
                // Note: this test will fail if the ListReport page doesn't show any data
                
                When.onThee_procurement_control_paramTList.onFilterBar().iExecuteSearch();
                
                Then.onThee_procurement_control_paramTList.onTable().iCheckRows();

                When.onThee_procurement_control_paramTList.onTable().iPressRow(0);
                Then.onThee_procurement_control_paramTObjectPage.iSeeThisPage();

            });

            opaTest("Teardown", function (Given, When, Then) { 
                // Cleanup
                Given.iTearDownMyApp();
            });
        }
    }

    return Journey;
});